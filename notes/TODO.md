# TODO

- make build.jai fail immediately if one of the examples fails to compile, instead of continuing
- unify tiles_fragment_shader_vk.glsl and tiles_fragment_shader.glsl
- try adding a mode where vertex/fragment are defined and emitted together
- rename glsl_example to opengl_example
- rename ShaderFuncs
